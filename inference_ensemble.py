import os
import torch
import numpy as np
# Skeleton script for Deep Ensembles
print('Inference ensemble script ready to be implemented. Load models, compute mean of variances (Aleatoric) and variance of means (Epistemic).')
